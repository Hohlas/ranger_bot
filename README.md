## Ranger trading bot

### описание
софт для [Ranger](https://www.app.ranger.finance/spot)
стратегия усреднения/пирамидинга для торговли WBTC с автоматическим управлением позициями

---

### 🆕 Стратегия "Усреднение / Пирамидинг"

Реализована сеточная стратегия для Spot торговли с поддержкой:
- ✅ Автоматическое усреднение при падении цены
- ✅ Пирамидинг при росте цены  
- ✅ Take Profit на каждую позицию
- ✅ Telegram уведомления
- ✅ Excel статистика операций

**Быстрый старт:**
1. Настройте параметры в `settings.py` (STEP, POSITION_SIZE_PERCENT, AGGR)
2. Заполните `input_data/private_keys.txt` (Solana ключи)
3. Файл `input_data/proxies.txt` может быть пустым или с реальными прокси
4. Запустите: `python main.py`
5. Выберите: **Averaging Market (Long)** (теперь первый пункт в меню!)

**Важно о прокси:**
- Файл `proxies.txt` может быть **пустым** (работа без прокси)
- Может содержать реальные прокси: `http://user:pass@ip:port`
- Шаблоны-подсказки автоматически игнорируются

---

### настройка

1. заполнить Solana приватники и прокси в папке `input_data`
2. в `settings.py` настройте софт под себя

---

### запуск

1. установить необходимые либы `pip install -r requirements.txt`
2. запустить софт `py main.py`
3. создать базу данных (*Create Database*)
4. стартуем необходимый режим

---

[ Hohla ](https://t.me/hohlas)

---
