
VERSION             = "2.37.0-debug-market-orders"  # Версия для отслеживания обновлений

# --- AVERAGING/PYRAMIDING STRATEGY SETTINGS ---
# Параметры стратегии "Усреднение/Пирамидинг" для Spot торговли

TRADING_ASSET           = "WBTC"           # Торгуемый актив (например: WBTC, WETH, SOL)
STEP                    = 200              # Шаг расстановки ордеров в долларах (расстояние между TP)
POSITION_SIZE_PERCENT   = 15                # Доля позиции в процентах от доступного баланса 
AGGR                    = 0.5              # Коэффициент агрессивности пирамидинга (0.1 - 1.0)
                                           # PWR = STEP * AGGR - расстояние для пирамидинга

# Формулы стратегии:
# - Первая позиция: маркет-ордер + TP на entry_price + STEP
# - Усреднение: если current_price < min_TP_price - STEP * 2
# - Пирамидинг: если current_price > max_TP_price - (STEP * AGGR)

SHUFFLE_WALLETS     = True                  # True | False - перемешивать ли кошельки
RETRY               = 3                     # кол-во попыток при ошибках / фейлах

TO_WAIT_TX          = 1                     # сколько минут ожидать транзакцию. если транза будет находится в пендинге после указанного времени то будет считатся зафейленной

# Тестовый режим отключен - фильтр ghost orders работает!
TEST_MODE           = False                 # Тестовый режим для создания лимитного ордера
TEST_LIMIT_ORDER_PRICE = 120000            # Фиксированная цена для тестового ордера

# ВРЕМЕННО ОТКЛЮЧЕНО: тестируем только TP ордера (без новых покупок)
ENABLE_TRADING      = True                 # Включить/выключить торговлю (покупки/продажи)

RPCS                = {
    'solana'    : 'https://api.mainnet-beta.solana.com',  # лучше поменять на рпс с https://www.quicknode.com/
}

# --- GENERAL SETTINGS ---
THREADS             = 0                     # одновременное количество аккаунтов которое может быть запущено
                                            # 0 = автоматически (по количеству аккаунтов в sol_privatekeys.txt)
                                            # >0 = использовать указанное значение (для ограничения при большом количестве аккаунтов)

MAIN_TOKEN          = "USDC"                # какой токен будет основным при трейдах (например USDC-Token-USDC)
SLEEP_AFTER_ACC     = [5, 10]              # задержка после каждого аккаунта 20-40 секунд


# Минимальные лимиты для Spot торговли (проверяются через API биржи)
MIN_ORDER_SIZE_BTC      = 0.0001           # Минимальный размер ордера в BTC
MIN_ORDER_NOTIONAL      = 5.0              # Минимальная стоимость ордера в USDC

# Настройки логирования стратегии
LOG_LEVEL               = "INFO"           # DEBUG | INFO | WARNING | ERROR
ENABLE_EXCEL_STATS      = True             # Сохранять статистику в Excel файл

# --- FALLBACK PRICE SOURCES ---
# Если основные источники цен недоступны, можно использовать альтернативные
# Приоритет: Jupiter v6 -> Jupiter v4 -> CoinGecko -> Binance
USE_FALLBACK_PRICES     = True             # Использовать альтернативные источники цен